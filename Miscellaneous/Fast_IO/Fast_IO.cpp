#include <iostream>
using namespace std;
//fast IO using cin, cout
int main()
{
    //add these lines at the beginning of the program to increase IO speed.
    ios::sync_with_stdio(false);
    cin.tie(0);
}